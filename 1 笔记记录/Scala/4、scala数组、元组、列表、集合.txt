
Scala 数组、元组、列表、有序列表、集合、迭代器、Map



元组：不可变；与列表一样，元组也是不可变的，但与列表不同的是元组可以包含不同类型的元素。
元组的值是通过将单个的值包含在圆括号中构成的。
val t = (1, "String", true, 1.22)

//访问元组中的元素
t._2; //表示访问元组第二个元素

val t = new Tuple3(1, 3.14, "Fred")

